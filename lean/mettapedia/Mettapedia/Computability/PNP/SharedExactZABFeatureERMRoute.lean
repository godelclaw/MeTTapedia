import Mettapedia.Computability.PNP.SharedExactZABFeatureERMRouteFamilies
import Mettapedia.Computability.PNP.SharedExactZABFeatureERMRouteTargets
import Mettapedia.Computability.PNP.SharedExactZABFeatureERMRouteRecovery

/-!
# P vs NP grassroots: ERM over shared-basis exact `(zfeat(z), a, b)` features

This file applies the generic ERM transport layer to the shared-basis exact
`z+a+b` affine-feature and sparse-threshold classes.
Compatibility umbrella for the ERM family, target, and recovery layers.
-/
