import Mettapedia.Computability.PNP.SharedExactZABFeatureERMInterfaceData
import Mettapedia.Computability.PNP.SharedExactZABFeatureERMInterfaceAffineFeature
import Mettapedia.Computability.PNP.SharedExactZABFeatureERMInterfaceSparseThreshold

/-!
# P vs NP grassroots: final ERM interface for shared exact `(zfeat(z), a, b)` features

This file packages the last manuscript-facing ingredients for ERM over the
shared exact `z+a+b` affine-feature and sparse-threshold classes.
Compatibility umbrella for the interface data and route-specific consequence
layers.
-/
