import Mettapedia.Computability.PNP.SharedExactABERMInterfaceConsequences

/-!
# P vs NP grassroots: final ERM interface for the shared-basis raw exact `(a, b)` route

This compatibility module preserves the original manuscript-facing import
surface. The declarations live in:

* `SharedExactABERMInterfaceData`
* `SharedExactABERMInterfaceConsequences`
-/
