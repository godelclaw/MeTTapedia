import Mettapedia.Computability.PNP.CruxKpolyBridgeCurrentStatus

/-!
# PNP crux synthesis umbrella

This module preserves the historical `CruxSynthesis` import path while the
semantic crux ledger layers live in smaller `Crux*` modules.  The remaining
`Kpoly` bridge synthesis content is in `CruxKpolyBridge`.
-/
