import Mettapedia.Computability.PNP.SharedExactABERMRouteRecovery

/-!
# P vs NP grassroots: ERM over shared-basis raw exact `(a, b)` classes

This compatibility module preserves the original import surface. The
declarations live in semantic modules:

* `SharedExactABERMRouteCore`
* `SharedExactABERMRouteTarget`
* `SharedExactABERMRouteRecovery`
-/
