import Mettapedia.Computability.PNP.CruxRandomizedResidualPromotedPacket

/-!
# PNP randomized-residual compatibility umbrella

The randomized-residual layer now lives in semantic modules:

- `CruxRandomizedResidualAnchors` for theorem anchors;
- `CruxRandomizedResidualCoverage` for packaged coverage propositions;
- `CruxRandomizedResidualPromotedPacket` for the packet-level promotion status.
-/
