import Mettapedia.Computability.PNP.CruxResidualPromotedPacket

/-!
# PNP residual-side-information compatibility umbrella

The residual-side-information layer now lives in semantic modules:

- `CruxResidualSideInformationAnchors` for theorem anchors;
- `CruxResidualSideInformationCoverage` for packaged coverage propositions;
- `CruxResidualPromotedPacket` for the packet-level promotion status.
-/
