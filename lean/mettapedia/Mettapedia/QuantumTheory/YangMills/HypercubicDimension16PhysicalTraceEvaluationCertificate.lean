import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk000
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk001
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk002
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk003
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk004
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk005
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk006
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk007
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk008
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk009
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk010
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk011
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk012
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk013
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk014
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk015
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk016
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk017
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk018
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk019
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk020
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk021
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk022
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk023
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk024
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk025
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk026
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk027
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk028
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk029
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk030
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk031
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk032
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk033
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk034
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk035
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk036
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk037
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk038
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk039
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk040
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk041
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk042
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk043
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk044
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk045
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk046
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk047
import Mettapedia.QuantumTheory.YangMills.HypercubicDimension16PhysicalTraceEvaluationChunk048

/-! # Kernel-checked exact standard trace evaluation matrix -/

set_option autoImplicit false
set_option maxRecDepth 10000000
set_option maxHeartbeats 0
noncomputable section

namespace Mettapedia
namespace QuantumTheory
namespace YangMills
namespace HypercubicDimension16PhysicalTraceEvaluationCertificate

open HypercubicDimension16PhysicalTraceEvaluationExecutable
open HypercubicDimension16PhysicalTraceEvaluationSemantics
open HypercubicDimension16PhysicalTraceConditioningExecutable

theorem unsignedEvaluation_replay
    (assignment freeIndex : Fin 98) :
    fourPairUnsignedReynoldsEvaluation assignment freeIndex =
      expectedUnsignedEvaluation assignment freeIndex := by
  by_cases h000 : assignment.1 < 2
  · let localIndex : Fin 2 := ⟨assignment.1 - 0, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk000.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk000.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk000.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h001 : assignment.1 < 4
  · let localIndex : Fin 2 := ⟨assignment.1 - 2, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk001.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk001.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk001.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h002 : assignment.1 < 6
  · let localIndex : Fin 2 := ⟨assignment.1 - 4, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk002.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk002.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk002.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h003 : assignment.1 < 8
  · let localIndex : Fin 2 := ⟨assignment.1 - 6, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk003.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk003.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk003.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h004 : assignment.1 < 10
  · let localIndex : Fin 2 := ⟨assignment.1 - 8, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk004.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk004.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk004.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h005 : assignment.1 < 12
  · let localIndex : Fin 2 := ⟨assignment.1 - 10, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk005.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk005.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk005.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h006 : assignment.1 < 14
  · let localIndex : Fin 2 := ⟨assignment.1 - 12, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk006.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk006.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk006.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h007 : assignment.1 < 16
  · let localIndex : Fin 2 := ⟨assignment.1 - 14, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk007.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk007.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk007.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h008 : assignment.1 < 18
  · let localIndex : Fin 2 := ⟨assignment.1 - 16, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk008.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk008.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk008.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h009 : assignment.1 < 20
  · let localIndex : Fin 2 := ⟨assignment.1 - 18, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk009.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk009.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk009.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h010 : assignment.1 < 22
  · let localIndex : Fin 2 := ⟨assignment.1 - 20, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk010.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk010.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk010.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h011 : assignment.1 < 24
  · let localIndex : Fin 2 := ⟨assignment.1 - 22, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk011.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk011.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk011.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h012 : assignment.1 < 26
  · let localIndex : Fin 2 := ⟨assignment.1 - 24, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk012.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk012.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk012.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h013 : assignment.1 < 28
  · let localIndex : Fin 2 := ⟨assignment.1 - 26, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk013.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk013.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk013.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h014 : assignment.1 < 30
  · let localIndex : Fin 2 := ⟨assignment.1 - 28, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk014.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk014.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk014.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h015 : assignment.1 < 32
  · let localIndex : Fin 2 := ⟨assignment.1 - 30, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk015.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk015.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk015.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h016 : assignment.1 < 34
  · let localIndex : Fin 2 := ⟨assignment.1 - 32, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk016.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk016.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk016.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h017 : assignment.1 < 36
  · let localIndex : Fin 2 := ⟨assignment.1 - 34, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk017.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk017.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk017.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h018 : assignment.1 < 38
  · let localIndex : Fin 2 := ⟨assignment.1 - 36, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk018.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk018.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk018.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h019 : assignment.1 < 40
  · let localIndex : Fin 2 := ⟨assignment.1 - 38, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk019.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk019.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk019.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h020 : assignment.1 < 42
  · let localIndex : Fin 2 := ⟨assignment.1 - 40, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk020.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk020.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk020.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h021 : assignment.1 < 44
  · let localIndex : Fin 2 := ⟨assignment.1 - 42, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk021.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk021.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk021.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h022 : assignment.1 < 46
  · let localIndex : Fin 2 := ⟨assignment.1 - 44, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk022.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk022.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk022.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h023 : assignment.1 < 48
  · let localIndex : Fin 2 := ⟨assignment.1 - 46, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk023.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk023.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk023.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h024 : assignment.1 < 50
  · let localIndex : Fin 2 := ⟨assignment.1 - 48, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk024.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk024.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk024.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h025 : assignment.1 < 52
  · let localIndex : Fin 2 := ⟨assignment.1 - 50, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk025.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk025.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk025.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h026 : assignment.1 < 54
  · let localIndex : Fin 2 := ⟨assignment.1 - 52, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk026.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk026.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk026.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h027 : assignment.1 < 56
  · let localIndex : Fin 2 := ⟨assignment.1 - 54, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk027.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk027.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk027.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h028 : assignment.1 < 58
  · let localIndex : Fin 2 := ⟨assignment.1 - 56, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk028.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk028.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk028.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h029 : assignment.1 < 60
  · let localIndex : Fin 2 := ⟨assignment.1 - 58, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk029.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk029.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk029.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h030 : assignment.1 < 62
  · let localIndex : Fin 2 := ⟨assignment.1 - 60, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk030.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk030.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk030.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h031 : assignment.1 < 64
  · let localIndex : Fin 2 := ⟨assignment.1 - 62, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk031.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk031.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk031.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h032 : assignment.1 < 66
  · let localIndex : Fin 2 := ⟨assignment.1 - 64, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk032.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk032.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk032.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h033 : assignment.1 < 68
  · let localIndex : Fin 2 := ⟨assignment.1 - 66, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk033.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk033.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk033.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h034 : assignment.1 < 70
  · let localIndex : Fin 2 := ⟨assignment.1 - 68, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk034.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk034.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk034.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h035 : assignment.1 < 72
  · let localIndex : Fin 2 := ⟨assignment.1 - 70, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk035.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk035.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk035.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h036 : assignment.1 < 74
  · let localIndex : Fin 2 := ⟨assignment.1 - 72, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk036.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk036.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk036.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h037 : assignment.1 < 76
  · let localIndex : Fin 2 := ⟨assignment.1 - 74, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk037.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk037.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk037.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h038 : assignment.1 < 78
  · let localIndex : Fin 2 := ⟨assignment.1 - 76, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk038.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk038.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk038.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h039 : assignment.1 < 80
  · let localIndex : Fin 2 := ⟨assignment.1 - 78, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk039.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk039.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk039.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h040 : assignment.1 < 82
  · let localIndex : Fin 2 := ⟨assignment.1 - 80, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk040.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk040.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk040.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h041 : assignment.1 < 84
  · let localIndex : Fin 2 := ⟨assignment.1 - 82, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk041.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk041.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk041.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h042 : assignment.1 < 86
  · let localIndex : Fin 2 := ⟨assignment.1 - 84, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk042.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk042.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk042.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h043 : assignment.1 < 88
  · let localIndex : Fin 2 := ⟨assignment.1 - 86, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk043.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk043.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk043.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h044 : assignment.1 < 90
  · let localIndex : Fin 2 := ⟨assignment.1 - 88, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk044.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk044.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk044.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h045 : assignment.1 < 92
  · let localIndex : Fin 2 := ⟨assignment.1 - 90, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk045.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk045.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk045.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h046 : assignment.1 < 94
  · let localIndex : Fin 2 := ⟨assignment.1 - 92, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk046.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk046.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk046.chunk_unsigned_evaluations localIndex freeIndex
  by_cases h047 : assignment.1 < 96
  · let localIndex : Fin 2 := ⟨assignment.1 - 94, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk047.assignmentIndex localIndex = assignment := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk047.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk047.chunk_unsigned_evaluations localIndex freeIndex
  let localIndex : Fin 2 := ⟨assignment.1 - 96, by have := assignment.isLt; omega⟩
  have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk048.assignmentIndex localIndex = assignment := by
    apply Fin.ext
    simp [HypercubicDimension16PhysicalTraceEvaluationChunk048.assignmentIndex, localIndex] <;> omega
  rw [← hequal]
  exact HypercubicDimension16PhysicalTraceEvaluationChunk048.chunk_unsigned_evaluations localIndex freeIndex

theorem evaluationInverse_replay (row column : Fin 98) :
    (∑ middle : Fin 98,
      decodedEvaluationInverse row middle *
        expectedUnsignedEvaluation middle column) =
      if row = column then 1 else 0 := by
  by_cases h000 : row.1 < 2
  · let localIndex : Fin 2 := ⟨row.1 - 0, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk000.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk000.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk000.chunk_evaluation_inverse localIndex column
  by_cases h001 : row.1 < 4
  · let localIndex : Fin 2 := ⟨row.1 - 2, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk001.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk001.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk001.chunk_evaluation_inverse localIndex column
  by_cases h002 : row.1 < 6
  · let localIndex : Fin 2 := ⟨row.1 - 4, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk002.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk002.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk002.chunk_evaluation_inverse localIndex column
  by_cases h003 : row.1 < 8
  · let localIndex : Fin 2 := ⟨row.1 - 6, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk003.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk003.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk003.chunk_evaluation_inverse localIndex column
  by_cases h004 : row.1 < 10
  · let localIndex : Fin 2 := ⟨row.1 - 8, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk004.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk004.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk004.chunk_evaluation_inverse localIndex column
  by_cases h005 : row.1 < 12
  · let localIndex : Fin 2 := ⟨row.1 - 10, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk005.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk005.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk005.chunk_evaluation_inverse localIndex column
  by_cases h006 : row.1 < 14
  · let localIndex : Fin 2 := ⟨row.1 - 12, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk006.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk006.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk006.chunk_evaluation_inverse localIndex column
  by_cases h007 : row.1 < 16
  · let localIndex : Fin 2 := ⟨row.1 - 14, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk007.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk007.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk007.chunk_evaluation_inverse localIndex column
  by_cases h008 : row.1 < 18
  · let localIndex : Fin 2 := ⟨row.1 - 16, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk008.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk008.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk008.chunk_evaluation_inverse localIndex column
  by_cases h009 : row.1 < 20
  · let localIndex : Fin 2 := ⟨row.1 - 18, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk009.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk009.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk009.chunk_evaluation_inverse localIndex column
  by_cases h010 : row.1 < 22
  · let localIndex : Fin 2 := ⟨row.1 - 20, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk010.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk010.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk010.chunk_evaluation_inverse localIndex column
  by_cases h011 : row.1 < 24
  · let localIndex : Fin 2 := ⟨row.1 - 22, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk011.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk011.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk011.chunk_evaluation_inverse localIndex column
  by_cases h012 : row.1 < 26
  · let localIndex : Fin 2 := ⟨row.1 - 24, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk012.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk012.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk012.chunk_evaluation_inverse localIndex column
  by_cases h013 : row.1 < 28
  · let localIndex : Fin 2 := ⟨row.1 - 26, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk013.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk013.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk013.chunk_evaluation_inverse localIndex column
  by_cases h014 : row.1 < 30
  · let localIndex : Fin 2 := ⟨row.1 - 28, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk014.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk014.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk014.chunk_evaluation_inverse localIndex column
  by_cases h015 : row.1 < 32
  · let localIndex : Fin 2 := ⟨row.1 - 30, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk015.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk015.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk015.chunk_evaluation_inverse localIndex column
  by_cases h016 : row.1 < 34
  · let localIndex : Fin 2 := ⟨row.1 - 32, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk016.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk016.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk016.chunk_evaluation_inverse localIndex column
  by_cases h017 : row.1 < 36
  · let localIndex : Fin 2 := ⟨row.1 - 34, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk017.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk017.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk017.chunk_evaluation_inverse localIndex column
  by_cases h018 : row.1 < 38
  · let localIndex : Fin 2 := ⟨row.1 - 36, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk018.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk018.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk018.chunk_evaluation_inverse localIndex column
  by_cases h019 : row.1 < 40
  · let localIndex : Fin 2 := ⟨row.1 - 38, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk019.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk019.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk019.chunk_evaluation_inverse localIndex column
  by_cases h020 : row.1 < 42
  · let localIndex : Fin 2 := ⟨row.1 - 40, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk020.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk020.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk020.chunk_evaluation_inverse localIndex column
  by_cases h021 : row.1 < 44
  · let localIndex : Fin 2 := ⟨row.1 - 42, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk021.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk021.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk021.chunk_evaluation_inverse localIndex column
  by_cases h022 : row.1 < 46
  · let localIndex : Fin 2 := ⟨row.1 - 44, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk022.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk022.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk022.chunk_evaluation_inverse localIndex column
  by_cases h023 : row.1 < 48
  · let localIndex : Fin 2 := ⟨row.1 - 46, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk023.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk023.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk023.chunk_evaluation_inverse localIndex column
  by_cases h024 : row.1 < 50
  · let localIndex : Fin 2 := ⟨row.1 - 48, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk024.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk024.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk024.chunk_evaluation_inverse localIndex column
  by_cases h025 : row.1 < 52
  · let localIndex : Fin 2 := ⟨row.1 - 50, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk025.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk025.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk025.chunk_evaluation_inverse localIndex column
  by_cases h026 : row.1 < 54
  · let localIndex : Fin 2 := ⟨row.1 - 52, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk026.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk026.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk026.chunk_evaluation_inverse localIndex column
  by_cases h027 : row.1 < 56
  · let localIndex : Fin 2 := ⟨row.1 - 54, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk027.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk027.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk027.chunk_evaluation_inverse localIndex column
  by_cases h028 : row.1 < 58
  · let localIndex : Fin 2 := ⟨row.1 - 56, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk028.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk028.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk028.chunk_evaluation_inverse localIndex column
  by_cases h029 : row.1 < 60
  · let localIndex : Fin 2 := ⟨row.1 - 58, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk029.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk029.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk029.chunk_evaluation_inverse localIndex column
  by_cases h030 : row.1 < 62
  · let localIndex : Fin 2 := ⟨row.1 - 60, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk030.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk030.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk030.chunk_evaluation_inverse localIndex column
  by_cases h031 : row.1 < 64
  · let localIndex : Fin 2 := ⟨row.1 - 62, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk031.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk031.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk031.chunk_evaluation_inverse localIndex column
  by_cases h032 : row.1 < 66
  · let localIndex : Fin 2 := ⟨row.1 - 64, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk032.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk032.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk032.chunk_evaluation_inverse localIndex column
  by_cases h033 : row.1 < 68
  · let localIndex : Fin 2 := ⟨row.1 - 66, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk033.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk033.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk033.chunk_evaluation_inverse localIndex column
  by_cases h034 : row.1 < 70
  · let localIndex : Fin 2 := ⟨row.1 - 68, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk034.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk034.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk034.chunk_evaluation_inverse localIndex column
  by_cases h035 : row.1 < 72
  · let localIndex : Fin 2 := ⟨row.1 - 70, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk035.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk035.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk035.chunk_evaluation_inverse localIndex column
  by_cases h036 : row.1 < 74
  · let localIndex : Fin 2 := ⟨row.1 - 72, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk036.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk036.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk036.chunk_evaluation_inverse localIndex column
  by_cases h037 : row.1 < 76
  · let localIndex : Fin 2 := ⟨row.1 - 74, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk037.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk037.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk037.chunk_evaluation_inverse localIndex column
  by_cases h038 : row.1 < 78
  · let localIndex : Fin 2 := ⟨row.1 - 76, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk038.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk038.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk038.chunk_evaluation_inverse localIndex column
  by_cases h039 : row.1 < 80
  · let localIndex : Fin 2 := ⟨row.1 - 78, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk039.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk039.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk039.chunk_evaluation_inverse localIndex column
  by_cases h040 : row.1 < 82
  · let localIndex : Fin 2 := ⟨row.1 - 80, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk040.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk040.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk040.chunk_evaluation_inverse localIndex column
  by_cases h041 : row.1 < 84
  · let localIndex : Fin 2 := ⟨row.1 - 82, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk041.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk041.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk041.chunk_evaluation_inverse localIndex column
  by_cases h042 : row.1 < 86
  · let localIndex : Fin 2 := ⟨row.1 - 84, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk042.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk042.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk042.chunk_evaluation_inverse localIndex column
  by_cases h043 : row.1 < 88
  · let localIndex : Fin 2 := ⟨row.1 - 86, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk043.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk043.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk043.chunk_evaluation_inverse localIndex column
  by_cases h044 : row.1 < 90
  · let localIndex : Fin 2 := ⟨row.1 - 88, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk044.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk044.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk044.chunk_evaluation_inverse localIndex column
  by_cases h045 : row.1 < 92
  · let localIndex : Fin 2 := ⟨row.1 - 90, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk045.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk045.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk045.chunk_evaluation_inverse localIndex column
  by_cases h046 : row.1 < 94
  · let localIndex : Fin 2 := ⟨row.1 - 92, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk046.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk046.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk046.chunk_evaluation_inverse localIndex column
  by_cases h047 : row.1 < 96
  · let localIndex : Fin 2 := ⟨row.1 - 94, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk047.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk047.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk047.chunk_evaluation_inverse localIndex column
  let localIndex : Fin 2 := ⟨row.1 - 96, by have := row.isLt; omega⟩
  have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk048.assignmentIndex localIndex = row := by
    apply Fin.ext
    simp [HypercubicDimension16PhysicalTraceEvaluationChunk048.assignmentIndex, localIndex] <;> omega
  rw [← hequal]
  exact HypercubicDimension16PhysicalTraceEvaluationChunk048.chunk_evaluation_inverse localIndex column

theorem evaluationInverse_row_l1_eq_data (row : Fin 98) :
    decodedEvaluationInverseRowL1 row =
      decodedEncodedInverseRowL1 row := by
  by_cases h000 : row.1 < 2
  · let localIndex : Fin 2 := ⟨row.1 - 0, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk000.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk000.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk000.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk000.chunk_inverse_row_l1_replay localIndex)
  by_cases h001 : row.1 < 4
  · let localIndex : Fin 2 := ⟨row.1 - 2, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk001.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk001.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk001.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk001.chunk_inverse_row_l1_replay localIndex)
  by_cases h002 : row.1 < 6
  · let localIndex : Fin 2 := ⟨row.1 - 4, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk002.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk002.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk002.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk002.chunk_inverse_row_l1_replay localIndex)
  by_cases h003 : row.1 < 8
  · let localIndex : Fin 2 := ⟨row.1 - 6, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk003.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk003.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk003.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk003.chunk_inverse_row_l1_replay localIndex)
  by_cases h004 : row.1 < 10
  · let localIndex : Fin 2 := ⟨row.1 - 8, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk004.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk004.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk004.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk004.chunk_inverse_row_l1_replay localIndex)
  by_cases h005 : row.1 < 12
  · let localIndex : Fin 2 := ⟨row.1 - 10, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk005.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk005.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk005.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk005.chunk_inverse_row_l1_replay localIndex)
  by_cases h006 : row.1 < 14
  · let localIndex : Fin 2 := ⟨row.1 - 12, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk006.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk006.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk006.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk006.chunk_inverse_row_l1_replay localIndex)
  by_cases h007 : row.1 < 16
  · let localIndex : Fin 2 := ⟨row.1 - 14, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk007.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk007.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk007.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk007.chunk_inverse_row_l1_replay localIndex)
  by_cases h008 : row.1 < 18
  · let localIndex : Fin 2 := ⟨row.1 - 16, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk008.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk008.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk008.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk008.chunk_inverse_row_l1_replay localIndex)
  by_cases h009 : row.1 < 20
  · let localIndex : Fin 2 := ⟨row.1 - 18, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk009.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk009.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk009.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk009.chunk_inverse_row_l1_replay localIndex)
  by_cases h010 : row.1 < 22
  · let localIndex : Fin 2 := ⟨row.1 - 20, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk010.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk010.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk010.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk010.chunk_inverse_row_l1_replay localIndex)
  by_cases h011 : row.1 < 24
  · let localIndex : Fin 2 := ⟨row.1 - 22, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk011.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk011.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk011.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk011.chunk_inverse_row_l1_replay localIndex)
  by_cases h012 : row.1 < 26
  · let localIndex : Fin 2 := ⟨row.1 - 24, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk012.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk012.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk012.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk012.chunk_inverse_row_l1_replay localIndex)
  by_cases h013 : row.1 < 28
  · let localIndex : Fin 2 := ⟨row.1 - 26, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk013.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk013.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk013.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk013.chunk_inverse_row_l1_replay localIndex)
  by_cases h014 : row.1 < 30
  · let localIndex : Fin 2 := ⟨row.1 - 28, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk014.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk014.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk014.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk014.chunk_inverse_row_l1_replay localIndex)
  by_cases h015 : row.1 < 32
  · let localIndex : Fin 2 := ⟨row.1 - 30, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk015.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk015.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk015.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk015.chunk_inverse_row_l1_replay localIndex)
  by_cases h016 : row.1 < 34
  · let localIndex : Fin 2 := ⟨row.1 - 32, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk016.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk016.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk016.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk016.chunk_inverse_row_l1_replay localIndex)
  by_cases h017 : row.1 < 36
  · let localIndex : Fin 2 := ⟨row.1 - 34, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk017.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk017.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk017.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk017.chunk_inverse_row_l1_replay localIndex)
  by_cases h018 : row.1 < 38
  · let localIndex : Fin 2 := ⟨row.1 - 36, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk018.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk018.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk018.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk018.chunk_inverse_row_l1_replay localIndex)
  by_cases h019 : row.1 < 40
  · let localIndex : Fin 2 := ⟨row.1 - 38, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk019.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk019.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk019.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk019.chunk_inverse_row_l1_replay localIndex)
  by_cases h020 : row.1 < 42
  · let localIndex : Fin 2 := ⟨row.1 - 40, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk020.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk020.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk020.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk020.chunk_inverse_row_l1_replay localIndex)
  by_cases h021 : row.1 < 44
  · let localIndex : Fin 2 := ⟨row.1 - 42, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk021.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk021.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk021.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk021.chunk_inverse_row_l1_replay localIndex)
  by_cases h022 : row.1 < 46
  · let localIndex : Fin 2 := ⟨row.1 - 44, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk022.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk022.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk022.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk022.chunk_inverse_row_l1_replay localIndex)
  by_cases h023 : row.1 < 48
  · let localIndex : Fin 2 := ⟨row.1 - 46, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk023.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk023.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk023.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk023.chunk_inverse_row_l1_replay localIndex)
  by_cases h024 : row.1 < 50
  · let localIndex : Fin 2 := ⟨row.1 - 48, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk024.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk024.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk024.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk024.chunk_inverse_row_l1_replay localIndex)
  by_cases h025 : row.1 < 52
  · let localIndex : Fin 2 := ⟨row.1 - 50, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk025.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk025.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk025.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk025.chunk_inverse_row_l1_replay localIndex)
  by_cases h026 : row.1 < 54
  · let localIndex : Fin 2 := ⟨row.1 - 52, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk026.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk026.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk026.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk026.chunk_inverse_row_l1_replay localIndex)
  by_cases h027 : row.1 < 56
  · let localIndex : Fin 2 := ⟨row.1 - 54, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk027.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk027.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk027.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk027.chunk_inverse_row_l1_replay localIndex)
  by_cases h028 : row.1 < 58
  · let localIndex : Fin 2 := ⟨row.1 - 56, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk028.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk028.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk028.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk028.chunk_inverse_row_l1_replay localIndex)
  by_cases h029 : row.1 < 60
  · let localIndex : Fin 2 := ⟨row.1 - 58, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk029.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk029.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk029.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk029.chunk_inverse_row_l1_replay localIndex)
  by_cases h030 : row.1 < 62
  · let localIndex : Fin 2 := ⟨row.1 - 60, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk030.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk030.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk030.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk030.chunk_inverse_row_l1_replay localIndex)
  by_cases h031 : row.1 < 64
  · let localIndex : Fin 2 := ⟨row.1 - 62, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk031.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk031.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk031.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk031.chunk_inverse_row_l1_replay localIndex)
  by_cases h032 : row.1 < 66
  · let localIndex : Fin 2 := ⟨row.1 - 64, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk032.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk032.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk032.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk032.chunk_inverse_row_l1_replay localIndex)
  by_cases h033 : row.1 < 68
  · let localIndex : Fin 2 := ⟨row.1 - 66, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk033.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk033.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk033.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk033.chunk_inverse_row_l1_replay localIndex)
  by_cases h034 : row.1 < 70
  · let localIndex : Fin 2 := ⟨row.1 - 68, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk034.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk034.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk034.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk034.chunk_inverse_row_l1_replay localIndex)
  by_cases h035 : row.1 < 72
  · let localIndex : Fin 2 := ⟨row.1 - 70, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk035.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk035.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk035.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk035.chunk_inverse_row_l1_replay localIndex)
  by_cases h036 : row.1 < 74
  · let localIndex : Fin 2 := ⟨row.1 - 72, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk036.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk036.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk036.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk036.chunk_inverse_row_l1_replay localIndex)
  by_cases h037 : row.1 < 76
  · let localIndex : Fin 2 := ⟨row.1 - 74, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk037.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk037.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk037.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk037.chunk_inverse_row_l1_replay localIndex)
  by_cases h038 : row.1 < 78
  · let localIndex : Fin 2 := ⟨row.1 - 76, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk038.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk038.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk038.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk038.chunk_inverse_row_l1_replay localIndex)
  by_cases h039 : row.1 < 80
  · let localIndex : Fin 2 := ⟨row.1 - 78, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk039.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk039.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk039.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk039.chunk_inverse_row_l1_replay localIndex)
  by_cases h040 : row.1 < 82
  · let localIndex : Fin 2 := ⟨row.1 - 80, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk040.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk040.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk040.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk040.chunk_inverse_row_l1_replay localIndex)
  by_cases h041 : row.1 < 84
  · let localIndex : Fin 2 := ⟨row.1 - 82, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk041.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk041.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk041.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk041.chunk_inverse_row_l1_replay localIndex)
  by_cases h042 : row.1 < 86
  · let localIndex : Fin 2 := ⟨row.1 - 84, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk042.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk042.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk042.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk042.chunk_inverse_row_l1_replay localIndex)
  by_cases h043 : row.1 < 88
  · let localIndex : Fin 2 := ⟨row.1 - 86, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk043.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk043.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk043.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk043.chunk_inverse_row_l1_replay localIndex)
  by_cases h044 : row.1 < 90
  · let localIndex : Fin 2 := ⟨row.1 - 88, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk044.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk044.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk044.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk044.chunk_inverse_row_l1_replay localIndex)
  by_cases h045 : row.1 < 92
  · let localIndex : Fin 2 := ⟨row.1 - 90, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk045.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk045.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk045.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk045.chunk_inverse_row_l1_replay localIndex)
  by_cases h046 : row.1 < 94
  · let localIndex : Fin 2 := ⟨row.1 - 92, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk046.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk046.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk046.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk046.chunk_inverse_row_l1_replay localIndex)
  by_cases h047 : row.1 < 96
  · let localIndex : Fin 2 := ⟨row.1 - 94, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk047.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk047.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
      (HypercubicDimension16PhysicalTraceEvaluationChunk047.assignmentIndex localIndex)
      (HypercubicDimension16PhysicalTraceEvaluationChunk047.chunk_inverse_row_l1_replay localIndex)
  let localIndex : Fin 2 := ⟨row.1 - 96, by have := row.isLt; omega⟩
  have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk048.assignmentIndex localIndex = row := by
    apply Fin.ext
    simp [HypercubicDimension16PhysicalTraceEvaluationChunk048.assignmentIndex, localIndex] <;> omega
  rw [← hequal]
  exact decodedEvaluationInverseRowL1_eq_encoded_of_replay
    (HypercubicDimension16PhysicalTraceEvaluationChunk048.assignmentIndex localIndex)
    (HypercubicDimension16PhysicalTraceEvaluationChunk048.chunk_inverse_row_l1_replay localIndex)

theorem evaluationInverse_row_l1_bound (row : Fin 98) :
    EncodedInverseRowL1Bound row := by
  by_cases h000 : row.1 < 2
  · let localIndex : Fin 2 := ⟨row.1 - 0, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk000.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk000.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk000.chunk_inverse_row_l1_bound localIndex
  by_cases h001 : row.1 < 4
  · let localIndex : Fin 2 := ⟨row.1 - 2, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk001.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk001.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk001.chunk_inverse_row_l1_bound localIndex
  by_cases h002 : row.1 < 6
  · let localIndex : Fin 2 := ⟨row.1 - 4, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk002.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk002.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk002.chunk_inverse_row_l1_bound localIndex
  by_cases h003 : row.1 < 8
  · let localIndex : Fin 2 := ⟨row.1 - 6, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk003.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk003.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk003.chunk_inverse_row_l1_bound localIndex
  by_cases h004 : row.1 < 10
  · let localIndex : Fin 2 := ⟨row.1 - 8, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk004.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk004.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk004.chunk_inverse_row_l1_bound localIndex
  by_cases h005 : row.1 < 12
  · let localIndex : Fin 2 := ⟨row.1 - 10, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk005.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk005.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk005.chunk_inverse_row_l1_bound localIndex
  by_cases h006 : row.1 < 14
  · let localIndex : Fin 2 := ⟨row.1 - 12, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk006.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk006.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk006.chunk_inverse_row_l1_bound localIndex
  by_cases h007 : row.1 < 16
  · let localIndex : Fin 2 := ⟨row.1 - 14, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk007.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk007.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk007.chunk_inverse_row_l1_bound localIndex
  by_cases h008 : row.1 < 18
  · let localIndex : Fin 2 := ⟨row.1 - 16, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk008.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk008.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk008.chunk_inverse_row_l1_bound localIndex
  by_cases h009 : row.1 < 20
  · let localIndex : Fin 2 := ⟨row.1 - 18, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk009.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk009.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk009.chunk_inverse_row_l1_bound localIndex
  by_cases h010 : row.1 < 22
  · let localIndex : Fin 2 := ⟨row.1 - 20, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk010.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk010.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk010.chunk_inverse_row_l1_bound localIndex
  by_cases h011 : row.1 < 24
  · let localIndex : Fin 2 := ⟨row.1 - 22, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk011.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk011.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk011.chunk_inverse_row_l1_bound localIndex
  by_cases h012 : row.1 < 26
  · let localIndex : Fin 2 := ⟨row.1 - 24, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk012.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk012.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk012.chunk_inverse_row_l1_bound localIndex
  by_cases h013 : row.1 < 28
  · let localIndex : Fin 2 := ⟨row.1 - 26, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk013.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk013.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk013.chunk_inverse_row_l1_bound localIndex
  by_cases h014 : row.1 < 30
  · let localIndex : Fin 2 := ⟨row.1 - 28, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk014.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk014.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk014.chunk_inverse_row_l1_bound localIndex
  by_cases h015 : row.1 < 32
  · let localIndex : Fin 2 := ⟨row.1 - 30, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk015.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk015.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk015.chunk_inverse_row_l1_bound localIndex
  by_cases h016 : row.1 < 34
  · let localIndex : Fin 2 := ⟨row.1 - 32, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk016.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk016.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk016.chunk_inverse_row_l1_bound localIndex
  by_cases h017 : row.1 < 36
  · let localIndex : Fin 2 := ⟨row.1 - 34, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk017.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk017.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk017.chunk_inverse_row_l1_bound localIndex
  by_cases h018 : row.1 < 38
  · let localIndex : Fin 2 := ⟨row.1 - 36, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk018.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk018.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk018.chunk_inverse_row_l1_bound localIndex
  by_cases h019 : row.1 < 40
  · let localIndex : Fin 2 := ⟨row.1 - 38, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk019.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk019.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk019.chunk_inverse_row_l1_bound localIndex
  by_cases h020 : row.1 < 42
  · let localIndex : Fin 2 := ⟨row.1 - 40, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk020.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk020.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk020.chunk_inverse_row_l1_bound localIndex
  by_cases h021 : row.1 < 44
  · let localIndex : Fin 2 := ⟨row.1 - 42, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk021.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk021.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk021.chunk_inverse_row_l1_bound localIndex
  by_cases h022 : row.1 < 46
  · let localIndex : Fin 2 := ⟨row.1 - 44, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk022.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk022.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk022.chunk_inverse_row_l1_bound localIndex
  by_cases h023 : row.1 < 48
  · let localIndex : Fin 2 := ⟨row.1 - 46, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk023.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk023.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk023.chunk_inverse_row_l1_bound localIndex
  by_cases h024 : row.1 < 50
  · let localIndex : Fin 2 := ⟨row.1 - 48, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk024.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk024.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk024.chunk_inverse_row_l1_bound localIndex
  by_cases h025 : row.1 < 52
  · let localIndex : Fin 2 := ⟨row.1 - 50, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk025.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk025.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk025.chunk_inverse_row_l1_bound localIndex
  by_cases h026 : row.1 < 54
  · let localIndex : Fin 2 := ⟨row.1 - 52, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk026.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk026.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk026.chunk_inverse_row_l1_bound localIndex
  by_cases h027 : row.1 < 56
  · let localIndex : Fin 2 := ⟨row.1 - 54, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk027.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk027.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk027.chunk_inverse_row_l1_bound localIndex
  by_cases h028 : row.1 < 58
  · let localIndex : Fin 2 := ⟨row.1 - 56, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk028.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk028.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk028.chunk_inverse_row_l1_bound localIndex
  by_cases h029 : row.1 < 60
  · let localIndex : Fin 2 := ⟨row.1 - 58, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk029.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk029.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk029.chunk_inverse_row_l1_bound localIndex
  by_cases h030 : row.1 < 62
  · let localIndex : Fin 2 := ⟨row.1 - 60, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk030.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk030.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk030.chunk_inverse_row_l1_bound localIndex
  by_cases h031 : row.1 < 64
  · let localIndex : Fin 2 := ⟨row.1 - 62, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk031.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk031.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk031.chunk_inverse_row_l1_bound localIndex
  by_cases h032 : row.1 < 66
  · let localIndex : Fin 2 := ⟨row.1 - 64, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk032.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk032.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk032.chunk_inverse_row_l1_bound localIndex
  by_cases h033 : row.1 < 68
  · let localIndex : Fin 2 := ⟨row.1 - 66, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk033.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk033.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk033.chunk_inverse_row_l1_bound localIndex
  by_cases h034 : row.1 < 70
  · let localIndex : Fin 2 := ⟨row.1 - 68, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk034.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk034.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk034.chunk_inverse_row_l1_bound localIndex
  by_cases h035 : row.1 < 72
  · let localIndex : Fin 2 := ⟨row.1 - 70, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk035.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk035.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk035.chunk_inverse_row_l1_bound localIndex
  by_cases h036 : row.1 < 74
  · let localIndex : Fin 2 := ⟨row.1 - 72, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk036.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk036.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk036.chunk_inverse_row_l1_bound localIndex
  by_cases h037 : row.1 < 76
  · let localIndex : Fin 2 := ⟨row.1 - 74, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk037.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk037.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk037.chunk_inverse_row_l1_bound localIndex
  by_cases h038 : row.1 < 78
  · let localIndex : Fin 2 := ⟨row.1 - 76, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk038.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk038.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk038.chunk_inverse_row_l1_bound localIndex
  by_cases h039 : row.1 < 80
  · let localIndex : Fin 2 := ⟨row.1 - 78, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk039.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk039.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk039.chunk_inverse_row_l1_bound localIndex
  by_cases h040 : row.1 < 82
  · let localIndex : Fin 2 := ⟨row.1 - 80, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk040.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk040.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk040.chunk_inverse_row_l1_bound localIndex
  by_cases h041 : row.1 < 84
  · let localIndex : Fin 2 := ⟨row.1 - 82, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk041.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk041.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk041.chunk_inverse_row_l1_bound localIndex
  by_cases h042 : row.1 < 86
  · let localIndex : Fin 2 := ⟨row.1 - 84, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk042.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk042.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk042.chunk_inverse_row_l1_bound localIndex
  by_cases h043 : row.1 < 88
  · let localIndex : Fin 2 := ⟨row.1 - 86, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk043.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk043.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk043.chunk_inverse_row_l1_bound localIndex
  by_cases h044 : row.1 < 90
  · let localIndex : Fin 2 := ⟨row.1 - 88, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk044.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk044.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk044.chunk_inverse_row_l1_bound localIndex
  by_cases h045 : row.1 < 92
  · let localIndex : Fin 2 := ⟨row.1 - 90, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk045.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk045.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk045.chunk_inverse_row_l1_bound localIndex
  by_cases h046 : row.1 < 94
  · let localIndex : Fin 2 := ⟨row.1 - 92, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk046.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk046.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk046.chunk_inverse_row_l1_bound localIndex
  by_cases h047 : row.1 < 96
  · let localIndex : Fin 2 := ⟨row.1 - 94, by omega⟩
    have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk047.assignmentIndex localIndex = row := by
      apply Fin.ext
      simp [HypercubicDimension16PhysicalTraceEvaluationChunk047.assignmentIndex, localIndex] <;> omega
    rw [← hequal]
    exact HypercubicDimension16PhysicalTraceEvaluationChunk047.chunk_inverse_row_l1_bound localIndex
  let localIndex : Fin 2 := ⟨row.1 - 96, by have := row.isLt; omega⟩
  have hequal : HypercubicDimension16PhysicalTraceEvaluationChunk048.assignmentIndex localIndex = row := by
    apply Fin.ext
    simp [HypercubicDimension16PhysicalTraceEvaluationChunk048.assignmentIndex, localIndex] <;> omega
  rw [← hequal]
  exact HypercubicDimension16PhysicalTraceEvaluationChunk048.chunk_inverse_row_l1_bound localIndex

/-- Sharp coefficient-sup upper bound for the explicit inverse. -/
theorem evaluationInverse_row_l1_le_real (row : Fin 98) :
    (decodedEvaluationInverseRowL1 row : ℝ) ≤
      (4913129483781 : ℝ) / 245314432 := by
  rw [evaluationInverse_row_l1_eq_data]
  exact decodedEncodedInverseRowL1_le_real_of_bound row
    (evaluationInverse_row_l1_bound row)

/-- Row 30 attains the exact coefficient-sup conditioning bound. -/
theorem evaluationInverse_row_l1_attained :
    decodedEvaluationInverseRowL1 ⟨30, by decide⟩ =
      evaluationInverseConditioningConstant := by
  rw [evaluationInverse_row_l1_eq_data]
  decide +kernel

end HypercubicDimension16PhysicalTraceEvaluationCertificate
end YangMills
end QuantumTheory
end Mettapedia
