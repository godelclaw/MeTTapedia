import Mettapedia.GraphTheory.FourColor.GoertzelV24AnnularCrosscutComplementCombinedCountOfBoundary
import Mettapedia.GraphTheory.FourColor.GoertzelV24AnnularCrosscutComplementSerialColoring
import Mettapedia.GraphTheory.FourColor.GoertzelV24AnnularCrosscutBoundaryRootedCompletion

/-!
# Full-profile Count support on the serial tangle carrier

The source's compositional semantics uses literal open tangles as morphisms.
The boundary-rooted five-coordinate `Count` was generated from colorings of
the complementary open rotation system.  This file reads every positive
entry as a Tait coloring of the same region's two-sided serial presentation
and identifies its two boundary words with the color projections of the two
full profiles.

This is a projection from full-profile support to color-word support, not a
replacement of the five-coordinate state.  It neither proves that arbitrary
compatible predecessor and Cell witnesses glue nor bounds a heterogeneous
reachable-profile closure.
-/

namespace Mettapedia.GraphTheory.FourColor

namespace GoertzelV24AnnularCrosscut

open GoertzelV24FaceOrbitIncidence
open GoertzelV24GraphDerivedCorridorCutProfile
open GoertzelV24OpenTangleComposition
open SimpleGraphDartRotation

variable {V : Type*} [Fintype V] [DecidableEq V]
  {G : SimpleGraph V} [DecidableRel G.Adj]

noncomputable section

attribute [local instance] graphEdgeSetDecidableEq

namespace SeparatedAlignedSimpleDualCrosscuts

/-- The boundary-rooted literal `Count` reader assigns a crossing edge the
color displayed by the opposite source boundary dart. -/
theorem sourceCrosscutComplementLiteralColorOnVertexSideOfBoundary_eq_boundaryWord
    (data : Data G)
    {start finish : AmbientFace
      (Finset.univ : Finset (OrbitFace data.toRotationSystem))}
    {hunique : PairwiseUniqueSharedInteriorEdges
      (orbitFaceBoundary data.toRotationSystem)
      (Finset.univ : Finset (OrbitFace data.toRotationSystem))}
    (pair : SeparatedAlignedSimpleDualCrosscuts
      (orbitFaceBoundary data.toRotationSystem)
      (Finset.univ : Finset (OrbitFace data.toRotationSystem))
      start finish hunique)
    (boundary : SourceCrosscutBoundaryData data pair)
    (coloring :
      (pair.sourceCrosscutComplementOpenRegionOfBoundary data boundary).EdgeColoring Color)
    (index : pair.SourceCrosscutBoundaryIndex) :
    pair.sourceCrosscutComplementLiteralColorOnVertexSideOfBoundary data boundary coloring
        (data.toRotationSystem.edgeOf
          (pair.sourceCrosscutBoundaryDartAt data boundary index).1.1) =
      pair.sourceCrosscutOutsideBoundaryWordOfBoundary data boundary coloring index := by
  change pair.sourceCrosscutBoundaryRootedLiteralColorOnVertexSide data boundary coloring
      (data.toRotationSystem.edgeOf
        (pair.sourceCrosscutBoundaryDartAt data boundary index).1.1) = _
  exact pair.sourceCrosscutBoundaryRootedLiteralColorOnVertexSide_eq_boundaryWord
    data boundary coloring index

/-- The left full profile generated by the literal `Count` reader displays
the complementary serial tangle's left boundary word. -/
theorem sourceCrosscutComplementLiteralOpenLeftProfileOfBoundary_colorWord
    (data : Data G)
    {start finish : AmbientFace
      (Finset.univ : Finset (OrbitFace data.toRotationSystem))}
    {hunique : PairwiseUniqueSharedInteriorEdges
      (orbitFaceBoundary data.toRotationSystem)
      (Finset.univ : Finset (OrbitFace data.toRotationSystem))}
    (pair : SeparatedAlignedSimpleDualCrosscuts
      (orbitFaceBoundary data.toRotationSystem)
      (Finset.univ : Finset (OrbitFace data.toRotationSystem))
      start finish hunique)
    (boundary : SourceCrosscutBoundaryData data pair)
    (coloring :
      (pair.sourceCrosscutComplementOpenRegionOfBoundary data boundary).EdgeColoring Color)
    (hcoloring :
      (pair.sourceCrosscutComplementOpenRegionOfBoundary data boundary).IsTaitEdgeColoring
        coloring) :
    pair.sourceCrosscutComplementInterfaceProfileLeftColorWord data boundary
        (pair.sourceCrosscutComplementLiteralOpenLeftProfileOfBoundary
          data boundary coloring hcoloring) =
      pair.sourceCrosscutComplementLeftBoundaryWordOfBoundary data boundary coloring := by
  funext step
  change ((pair.sourceCrosscutComplementLiteralOpenLeftProfileOfBoundary
      data boundary coloring hcoloring).edgeColor step).toColor = _
  calc
    ((pair.sourceCrosscutComplementLiteralOpenLeftProfileOfBoundary
        data boundary coloring hcoloring).edgeColor step).toColor =
        pair.sourceCrosscutComplementLiteralColorOnVertexSideOfBoundary
          data boundary coloring
          ((pair.sourceCrosscutComplementLeftCutData data boundary).crossingEdge step) := by
      unfold sourceCrosscutComplementLiteralOpenLeftProfileOfBoundary
      exact GraphCorridorCutData.regionalProfile_edgeColor_toColor _ _ _ step
    _ = pair.sourceCrosscutComplementLiteralColorOnVertexSideOfBoundary
          data boundary coloring (pair.left.crossingEdge hunique step) := by rfl
    _ = pair.sourceCrosscutComplementLiteralColorOnVertexSideOfBoundary
          data boundary coloring
          (data.toRotationSystem.edgeOf
            (pair.sourceCrosscutBoundaryDartAt data boundary (.inl step)).1.1) := by
      rw [pair.sourceCrosscutBoundaryDartAt_edgeOf data boundary (.inl step)]
      rfl
    _ = pair.sourceCrosscutOutsideBoundaryWordOfBoundary
          data boundary coloring (.inl step) :=
      pair.sourceCrosscutComplementLiteralColorOnVertexSideOfBoundary_eq_boundaryWord
        data boundary coloring (.inl step)

/-- The aligned right full profile generated by the literal `Count` reader
displays the complementary serial tangle's right boundary word. -/
theorem sourceCrosscutComplementLiteralOpenRightProfileOfBoundary_colorWord
    (data : Data G)
    {start finish : AmbientFace
      (Finset.univ : Finset (OrbitFace data.toRotationSystem))}
    {hunique : PairwiseUniqueSharedInteriorEdges
      (orbitFaceBoundary data.toRotationSystem)
      (Finset.univ : Finset (OrbitFace data.toRotationSystem))}
    (pair : SeparatedAlignedSimpleDualCrosscuts
      (orbitFaceBoundary data.toRotationSystem)
      (Finset.univ : Finset (OrbitFace data.toRotationSystem))
      start finish hunique)
    (boundary : SourceCrosscutBoundaryData data pair)
    (coloring :
      (pair.sourceCrosscutComplementOpenRegionOfBoundary data boundary).EdgeColoring Color)
    (hcoloring :
      (pair.sourceCrosscutComplementOpenRegionOfBoundary data boundary).IsTaitEdgeColoring
        coloring) :
    pair.sourceCrosscutComplementInterfaceProfileRightColorWord data boundary
        (pair.sourceCrosscutComplementLiteralOpenRightProfileOfBoundary
          data boundary coloring hcoloring) =
      pair.sourceCrosscutComplementRightBoundaryWordOfBoundary data boundary coloring := by
  funext rightStep
  let step : Fin pair.left.walk.length := Fin.cast pair.length_eq.symm rightStep
  have hcast : Fin.cast pair.length_eq step = rightStep := by
    apply Fin.ext
    rfl
  change ((pair.sourceCrosscutComplementLiteralOpenRightProfileOfBoundary
      data boundary coloring hcoloring).edgeColor step).toColor = _
  calc
    ((pair.sourceCrosscutComplementLiteralOpenRightProfileOfBoundary
        data boundary coloring hcoloring).edgeColor step).toColor =
        pair.sourceCrosscutComplementLiteralColorOnVertexSideOfBoundary
          data boundary coloring
          ((pair.sourceCrosscutComplementRightCutData data boundary).crossingEdge step) := by
      unfold sourceCrosscutComplementLiteralOpenRightProfileOfBoundary
      exact GraphCorridorCutData.regionalProfile_edgeColor_toColor _ _ _ step
    _ = pair.sourceCrosscutComplementLiteralColorOnVertexSideOfBoundary
          data boundary coloring
          (pair.right.crossingEdge hunique (Fin.cast pair.length_eq step)) := by rfl
    _ = pair.sourceCrosscutComplementLiteralColorOnVertexSideOfBoundary
          data boundary coloring (pair.right.crossingEdge hunique rightStep) := by rw [hcast]
    _ = pair.sourceCrosscutComplementLiteralColorOnVertexSideOfBoundary
          data boundary coloring
          (data.toRotationSystem.edgeOf
            (pair.sourceCrosscutBoundaryDartAt data boundary (.inr rightStep)).1.1) := by
      rw [pair.sourceCrosscutBoundaryDartAt_edgeOf data boundary (.inr rightStep)]
      rfl
    _ = pair.sourceCrosscutOutsideBoundaryWordOfBoundary
          data boundary coloring (.inr rightStep) :=
      pair.sourceCrosscutComplementLiteralColorOnVertexSideOfBoundary_eq_boundaryWord
        data boundary coloring (.inr rightStep)

/-- A positive full-profile `Count` entry supplies a Tait coloring of the
same complementary region's two-sided serial presentation, with exactly the
two displayed profile color words. -/
theorem exists_serialColoring_of_complementLiteralOpenProfileCountOfBoundary_pos
    (data : Data G)
    {start finish : AmbientFace
      (Finset.univ : Finset (OrbitFace data.toRotationSystem))}
    {hunique : PairwiseUniqueSharedInteriorEdges
      (orbitFaceBoundary data.toRotationSystem)
      (Finset.univ : Finset (OrbitFace data.toRotationSystem))}
    (pair : SeparatedAlignedSimpleDualCrosscuts
      (orbitFaceBoundary data.toRotationSystem)
      (Finset.univ : Finset (OrbitFace data.toRotationSystem))
      start finish hunique)
    (boundary : SourceCrosscutBoundaryData data pair)
    (left right : pair.SourceCrosscutComplementInterfaceProfile data boundary)
    (hpositive : 0 <
      pair.sourceCrosscutComplementLiteralOpenProfileCountOfBoundary
        data boundary left right) :
    ∃ serialColoring :
        (pair.sourceCrosscutComplementTwoSidedOpenTangleOfBoundary
          data boundary).Coloring,
      (pair.sourceCrosscutComplementTwoSidedOpenTangleOfBoundary
          data boundary).IsTaitColoring serialColoring ∧
        (pair.sourceCrosscutComplementTwoSidedOpenTangleOfBoundary
            data boundary).leftBoundaryColor serialColoring =
          pair.sourceCrosscutComplementInterfaceProfileLeftColorWord
            data boundary left ∧
        (pair.sourceCrosscutComplementTwoSidedOpenTangleOfBoundary
            data boundary).rightBoundaryColor serialColoring =
          pair.sourceCrosscutComplementInterfaceProfileRightColorWord
            data boundary right := by
  rcases (pair.sourceCrosscutComplementLiteralOpenProfileCountOfBoundary_pos_iff
      data boundary left right).1 hpositive with ⟨coloring, hleft, hright⟩
  let serialColoring := pair.sourceCrosscutComplementSerialColoringOfBoundary
    data boundary coloring.1
  refine ⟨serialColoring,
    pair.sourceCrosscutComplementSerialColoringOfBoundary_isTait
      data boundary coloring.1 coloring.2,
    ?_, ?_⟩
  · calc
      (pair.sourceCrosscutComplementTwoSidedOpenTangleOfBoundary
          data boundary).leftBoundaryColor serialColoring =
          pair.sourceCrosscutComplementLeftBoundaryWordOfBoundary
            data boundary coloring.1 := by
        funext step
        exact pair.sourceCrosscutComplementSerialColoringOfBoundary_leftBoundaryColor
          data boundary coloring.1 step
      _ = pair.sourceCrosscutComplementInterfaceProfileLeftColorWord
          data boundary
          (pair.sourceCrosscutComplementLiteralOpenLeftProfileOfBoundary
            data boundary coloring.1 coloring.2) := by
        symm
        exact pair.sourceCrosscutComplementLiteralOpenLeftProfileOfBoundary_colorWord
          data boundary coloring.1 coloring.2
      _ = pair.sourceCrosscutComplementInterfaceProfileLeftColorWord
          data boundary left := by rw [hleft]
  · calc
      (pair.sourceCrosscutComplementTwoSidedOpenTangleOfBoundary
          data boundary).rightBoundaryColor serialColoring =
          pair.sourceCrosscutComplementRightBoundaryWordOfBoundary
            data boundary coloring.1 := by
        funext step
        exact pair.sourceCrosscutComplementSerialColoringOfBoundary_rightBoundaryColor
          data boundary coloring.1 step
      _ = pair.sourceCrosscutComplementInterfaceProfileRightColorWord
          data boundary
          (pair.sourceCrosscutComplementLiteralOpenRightProfileOfBoundary
            data boundary coloring.1 coloring.2) := by
        symm
        exact pair.sourceCrosscutComplementLiteralOpenRightProfileOfBoundary_colorWord
          data boundary coloring.1 coloring.2
      _ = pair.sourceCrosscutComplementInterfaceProfileRightColorWord
          data boundary right := by rw [hright]

end SeparatedAlignedSimpleDualCrosscuts

end

end GoertzelV24AnnularCrosscut

end Mettapedia.GraphTheory.FourColor
