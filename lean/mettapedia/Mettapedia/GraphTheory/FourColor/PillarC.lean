import Mettapedia.GraphTheory.FourColor.GoertzelLemma814
import Mettapedia.GraphTheory.FourColor.GoertzelLemma814KempeAnchor
import Mettapedia.GraphTheory.FourColor.GoertzelLemma818SemanticBridge
import Mettapedia.GraphTheory.FourColor.GoertzelLemma818ComponentCertificate

namespace Mettapedia.GraphTheory.FourColor

/-!
# Focused Pillar-C build target

This module is the lightweight entry point for the live Pillar-C route.  It
imports the canonical gadget audit for Lemmas 8.14 and 8.15, the checked
Lemma 8.18 pointwise-preparation obstruction and chain-certificate API in
`GoertzelLemma814`, the length-two base/frontier semantic bridge for the
finite 20-mode DFA plan, and the compact component-certificate checker.

It intentionally does not import the archival FourColor aggregate, legacy
CAP5/Theorem49 route files, `GoertzelLemma818SemanticProgress`, or the
generated `GoertzelLemma818*LargeFiber*` certificate modules.  Use this target
for fast interface work on the local-to-global lift; build the larger
certificate/progress modules only when editing those certificate layers.
-/

end Mettapedia.GraphTheory.FourColor
